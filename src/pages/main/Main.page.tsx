import React from 'react';

import { Button } from "@geist-ui/react";

export const MainPage: React.FC = () => {
    return (
        <Button>
            This is test button
        </Button>
    );
};
